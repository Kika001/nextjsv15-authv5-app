export type IconName = "layout-dashboard" | "users" | "settings" | "life-buoy" | "user" | "lock" | "package" | "layout-grid";

export type SidebarNavItem = {
  title: string;
  href: string;
  icon: IconName;
};